plugins {
    id("com.android.application")
}

android {
    namespace "com.velir.minigame"
    compileSdk 35

    defaultConfig {
        applicationId "com.velir.minigame"
        minSdk 24
        targetSdk 35
        versionCode 3
        versionName "1.0.2"
        ndk {
            abiFilters "arm64-v8a"
        }
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile("proguard-android-optimize.txt"), "proguard-rules.pro"
        }
        debug {
            debuggable true
        }
    }

    externalNativeBuild {
        cmake {
            path file("src/main/cpp/CMakeLists.txt")
            version "3.31.6"
        }
    }

    sourceSets {
        main {
            assets.srcDirs = ["../../assets", "src/main/assets"]
        }
    }
}

tasks.register("syncGameAssets", Sync) {
    from("../../assets") {
        include "sprite-hero.png"
        include "medieval-rpg-main-character-d002.png"
        include "player_spritesheet.png"
        include "hero-idle-strip.png"
        include "hero-attack-strip.png"
        include "sprite-hero-attack.png"
        include "tile-ground.png"
        include "tile-stone-ground.png"
        include "tile.png"
        include "enemy-echo-basic.png"
        include "sprite-simple-enemy.png"
        include "ui-choir-map.png"
        include "sprite-quest-map.png"
    }
    into("src/main/assets")
}

tasks.named("preBuild").configure {
    dependsOn("syncGameAssets")
}
